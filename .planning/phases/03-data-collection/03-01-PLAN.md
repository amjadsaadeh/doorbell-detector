---
phase: 03-data-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data_collection/detector.py
  - tests/detector_test.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "Running with --save produces a timestamped WAV file in --save-dir after each detection"
    - "Running without --save produces no WAV files and allocates no ring buffer"
    - "The saved WAV contains pre-trigger audio (ring buffer) followed by post-trigger audio"
    - "--buffer-minutes controls ring buffer depth; default 0.5 min = 30 s of pre-trigger at 500 ms chunks"
    - "--post-trigger-seconds controls post-trigger duration; default 3.0 s"
    - "WAV filename is doorbell_YYYYMMDD_HHMMSS.wav inside --save-dir (default: recordings)"
    - "Disk I/O runs in a daemon thread; the capture loop is not blocked by file writes"
  artifacts:
    - path: "data_collection/detector.py"
      provides: "save_clip(), 4 new CLI args, ring buffer + threaded save wired into main()"
      contains: "def save_clip"
    - path: "tests/detector_test.py"
      provides: "Tests for new CLI args defaults/overrides and save_clip WAV validity"
      contains: "TestSaveClip"
  key_links:
    - from: "ring_buf (collections.deque in main)"
      to: "save_clip() daemon thread"
      via: "list(ring_buf) snapshot taken at detection time"
      pattern: "list\\(ring_buf\\)"
    - from: "post-trigger collection (stream.read loop in main)"
      to: "save_clip() daemon thread"
      via: "post_frames list concatenated with pre_frames"
      pattern: "post_frames"
    - from: "threading.Thread"
      to: "save_clip"
      via: "daemon=True, started on each detection when --save is active"
      pattern: "threading\\.Thread.*save_clip"
---

<objective>
Extend detector.py with optional WAV clip saving: a ring buffer accumulates pre-trigger
audio continuously; on each detection the buffer is snapshotted, post-trigger chunks are
collected from the stream, and a background thread writes the combined frames to a
timestamped WAV file using the stdlib wave module.

Purpose: Grow the labeled training dataset automatically — every genuine detection
produces a usable clip without operator intervention.
Output: Modified detector.py with save_clip() + 4 CLI args + ring buffer wiring;
extended detector_test.py covering the new behaviour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-script-foundation/01-01-SUMMARY.md
@.planning/phases/02-detection-notification/02-01-SUMMARY.md
@.planning/phases/02-detection-notification/02-02-SUMMARY.md

## Key interfaces the executor must know before editing

From data_collection/detector.py (current state after Phase 2):

```
Module-level constants (immutable — project contract):
  CHANNELS = 1
  FORMAT = pyaudio.paInt16
  RATE = 16000

Existing functions (do not rename or change signatures):
  load_template(path: str) -> np.ndarray
  find_device(p: pyaudio.PyAudio, device_name: str) -> int | None
  compute_score(audio_bytes: bytes, template: np.ndarray) -> float
  parse_args() -> argparse.Namespace
  setup_mqtt_publisher(args) -> mqtt.Client | None
  main() -> None

Existing imports (stdlib block at top):
  import argparse, datetime, logging, sys, time
  from math import gcd

Capture loop structure in main() (abbreviated):
  chunk = int(RATE * args.chunk_size_ms / 1000)
  last_detection_time: float = 0.0
  while True:
      data = stream.read(chunk, exception_on_overflow=False)
      score = compute_score(data, template)
      now = time.monotonic()
      if score >= args.threshold and (now - last_detection_time) >= args.cooldown_seconds:
          last_detection_time = now
          log.info("Doorbell detected! score=%.4f", score)
          if mqtt_client is not None:
              mqtt_client.publish(args.mqtt_detect_topic, ...)
```

From tests/detector_test.py (current state after Phase 2):
  Class TestComputeScore — tests compute_score()
  Class TestParseArgsNewFlags — tests --threshold and --cooldown-seconds defaults/overrides
  Pattern for parse helpers: sys.argv patching inside _parse() helper method
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: save_clip() function, stdlib imports, and new CLI args</name>
  <files>data_collection/detector.py, tests/detector_test.py</files>
  <behavior>
    - save_clip([], "/tmp/x.wav") raises no exception and creates a valid WAV with 0 bytes of audio
    - save_clip([b"\x00\x00" * 8000], "/tmp/x.wav") produces a readable WAV with nchannels=1, sampwidth=2, framerate=16000
    - parse_args() with only --template dummy.wav: args.save == False
    - parse_args() with only --template dummy.wav: args.save_dir == "recordings"
    - parse_args() with only --template dummy.wav: args.buffer_minutes == 0.5
    - parse_args() with only --template dummy.wav: args.post_trigger_seconds == 3.0
    - parse_args() with --save: args.save == True
    - parse_args() with --save-dir /tmp/clips: args.save_dir == "/tmp/clips"
    - parse_args() with --buffer-minutes 1.5: args.buffer_minutes == 1.5
    - parse_args() with --post-trigger-seconds 5.0: args.post_trigger_seconds == 5.0
  </behavior>
  <action>
    Write the tests first (RED), then implement until tests pass (GREEN).

    In tests/detector_test.py — add two new test classes after the existing ones:

    Class TestSaveClip: import tempfile and wave at the top of the test file.
    Write a setUp that creates a NamedTemporaryFile path (use tempfile.mktemp with .wav
    suffix; do NOT use NamedTemporaryFile directly because wave.open needs to open the
    path itself). In tearDown, remove the file if it exists.
    - test_empty_frames_creates_valid_wav: call save_clip([], tmp_path), then open with
      wave.open in "rb" mode and assert nchannels==1, getsampwidth()==2, getframerate()==16000
    - test_single_chunk_roundtrip: make one chunk of 8000 int16 zero samples as bytes,
      call save_clip([chunk_bytes], tmp_path), open result, assert getnframes()==8000
    - test_exception_logged_on_bad_path: call save_clip([], "/no/such/dir/x.wav") and
      confirm no exception propagates (save_clip must catch internally and log error)

    Class TestSaveArgs: reuse the _parse() helper pattern from TestParseArgsNewFlags.
    - test_save_default_false
    - test_save_dir_default
    - test_buffer_minutes_default
    - test_post_trigger_seconds_default
    - test_save_flag_sets_true
    - test_save_dir_override
    - test_buffer_minutes_override
    - test_post_trigger_seconds_override

    In data_collection/detector.py:

    1. Stdlib import block: add `import collections`, `import threading`, `import wave`
       on separate lines, inserted alphabetically after `import argparse` and before
       `import datetime`. Add `from pathlib import Path` after `from math import gcd`.

    2. Update the module-level docstring: remove "Phase 1 skeleton" and "Phase 2 will
       extend this" language. Replace with a concise description covering all three
       phases: CLI setup, cross-correlation detection, MQTT notification, and optional
       WAV clip saving.

    3. Add save_clip() immediately after compute_score() and before parse_args():

       def save_clip(frames: list, filepath: str) -> None:
           """Write PCM byte buffers to a WAV file. Runs safely in a daemon thread.

           Args:
               frames: List of raw PCM byte buffers (int16, mono, RATE Hz).
               filepath: Destination file path (must be writable).
           """
           try:
               with wave.open(filepath, "wb") as wf:
                   wf.setnchannels(CHANNELS)
                   wf.setsampwidth(2)   # int16 = 2 bytes per sample
                   wf.setframerate(RATE)
                   wf.writeframes(b"".join(frames))
               log.info("Saved WAV clip to %s (%d chunks)", filepath, len(frames))
           except Exception as exc:
               log.error("Failed to write clip to %s: %s", filepath, exc)

       Do NOT use fenced code blocks inside this action; the above is prose description
       of what to write, not code to paste verbatim.

    4. In parse_args(), after the existing MQTT argument group, add a new save group:

       save_group = parser.add_argument_group("Save clips")
       --save: store_true, help "Save a WAV clip to --save-dir on each detection."
       --save-dir: str, default "recordings",
           help "Directory for saved clips (default: recordings)."
       --buffer-minutes: float, default 0.5,
           help "Pre-trigger ring buffer size in minutes (default: 0.5)."
       --post-trigger-seconds: float, default 3.0,
           help "Post-trigger audio duration in seconds (default: 3.0)."

       These four args are additive — they must not break any existing arg or its default.
  </action>
  <verify>
    <automated>cd /home/orchid/projects/doorbell-detector && .venv/bin/python -m pytest tests/detector_test.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All existing tests still pass. TestSaveClip and TestSaveArgs pass (11 new test methods:
    3 in TestSaveClip + 8 in TestSaveArgs). save_clip() exists in detector.py. Four new
    save args present in parse_args() with correct defaults. No new pip dependencies
    introduced. grep confirms ring_buf, threading.Thread, and post_frames present in
    detector.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ring buffer init and detection-triggered async save in main()</name>
  <files>data_collection/detector.py</files>
  <action>
    Modify main() in data_collection/detector.py. All changes are additive — do not
    remove or reorder any existing logic.

    1. After the template-loading block and before the "Derived constants" comment, insert
       a save-mode setup block. This block runs only when args.save is True:

       Compute chunk_size_s = args.chunk_size_ms / 1000.0 (float).
       Create save_dir = Path(args.save_dir) and call save_dir.mkdir(parents=True,
       exist_ok=True).
       Compute buffer_chunks = max(1, int(args.buffer_minutes * 60 / chunk_size_s)).
       Create ring_buf: collections.deque = collections.deque(maxlen=buffer_chunks).
       Log: "Save mode enabled — ring buffer %.1f min (%d chunks), post-trigger %.1f s"
       with args.buffer_minutes, buffer_chunks, args.post_trigger_seconds.

       If args.save is False, set ring_buf = None and do not create save_dir.

       Note: chunk_size_s must be assigned before the existing `chunk = int(RATE *
       args.chunk_size_ms / 1000)` line or derived from it — keep chunk consistent.
       Simplest: compute chunk_size_s = args.chunk_size_ms / 1000.0 first, then
       chunk = int(RATE * chunk_size_s). This replaces the existing chunk derivation
       with an equivalent but named intermediate.

    2. In the capture loop, immediately after `data = stream.read(chunk, ...)`, add:
       if ring_buf is not None:
           ring_buf.append(data)

       This must come before the compute_score() call so every chunk is buffered
       regardless of detection outcome.

    3. Inside the detection branch (score >= threshold AND cooldown passed), after the
       existing log.info("Doorbell detected!") call and after the MQTT publish block,
       add a save block:

       if args.save and ring_buf is not None:
           ts = datetime.datetime.now().strftime("doorbell_%Y%m%d_%H%M%S.wav")
           filepath = str(save_dir / ts)
           pre_frames = list(ring_buf)
           post_chunks = max(1, int(args.post_trigger_seconds / chunk_size_s))
           post_frames = []
           for _ in range(post_chunks):
               try:
                   post_frames.append(stream.read(chunk, exception_on_overflow=False))
               except OSError:
                   log.warning("Buffer overflow during post-trigger collection")
                   break
           threading.Thread(
               target=save_clip,
               args=(pre_frames + post_frames, filepath),
               daemon=True,
           ).start()
           log.info(
               "Saving clip %s (%d pre + %d post chunks)",
               filepath, len(pre_frames), len(post_frames),
           )

       The post-trigger collection loop reads from the stream synchronously (blocking
       the main loop for post_trigger_seconds). This is acceptable because the cooldown
       period (default 10 s) far exceeds the post-trigger duration (default 3 s), so
       no detection will be missed. The thread handles the slow disk write so the main
       loop resumes after collection, not after writing.

    4. No changes needed to the finally: block — existing stream/PyAudio/MQTT teardown
       is correct. Daemon threads are killed automatically on process exit.

    Self-check before writing: confirm ring_buf is None when --save is not passed
    (no deque allocation), and that the new imports from Task 1 (collections, threading,
    wave, pathlib.Path) are already in the file before this task runs.
  </action>
  <verify>
    <automated>cd /home/orchid/projects/doorbell-detector && .venv/bin/python -m pytest tests/detector_test.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests pass. detector.py imports collections, threading, wave, Path. main() creates
    ring_buf only when --save is set. Capture loop appends to ring_buf each iteration.
    Detection branch snapshots ring_buf, collects post-trigger chunks, starts daemon thread.
    Running `python data_collection/detector.py --help` shows --save, --save-dir,
    --buffer-minutes, --post-trigger-seconds in the "Save clips" group.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> filesystem | --save-dir and --template are user-supplied paths used for file creation/reads |
| Capture loop -> daemon thread | Pre/post frames passed as in-memory list; no shared mutable state after handoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | --save-dir path | accept | Pi is a single-user local device; no remote callers supply this arg; operator is the file-system owner |
| T-03-02 | Denial of Service | Unbounded WAV writes filling disk | accept | Noted v2 concern; out of scope for v1; operator controls --cooldown-seconds to limit frequency |
| T-03-03 | Information Disclosure | WAV clips contain ambient audio | accept | Operator is the Pi owner; clips stay on local device; no network upload |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages — stdlib wave, threading, collections only; slopcheck not applicable |
</threat_model>

<verification>
## Phase 3 Overall Checks

1. `cd /home/orchid/projects/doorbell-detector && .venv/bin/python -m pytest tests/detector_test.py -v` — all tests pass including new TestSaveClip and TestSaveArgs.
2. `grep -n "def save_clip\|--save\|buffer_minutes\|post_trigger_seconds\|ring_buf\|threading.Thread" data_collection/detector.py` — all six patterns present.
3. `python data_collection/detector.py --help 2>&1 | grep -A6 "Save clips"` — shows all four save args with correct defaults.
4. REQUIREMENTS.md traceability: DATA-01 through DATA-04 status updated to Complete.
</verification>

<success_criteria>
1. `pytest tests/detector_test.py` passes — all pre-existing tests plus TestSaveClip (3 tests) and TestSaveArgs (8 tests).
2. Running `python data_collection/detector.py --template t.wav --save --save-dir /tmp/clips --buffer-minutes 0.1 --post-trigger-seconds 1.0` prints the "Save mode enabled" log line and accepts all arguments without error.
3. No new entries in requirements.txt — stdlib only.
4. ROADMAP.md Phase 3 plan list updated. STATE.md reflects phase 3 in progress.
</success_criteria>

<output>
Create `.planning/phases/03-data-collection/03-01-SUMMARY.md` when done.
</output>
