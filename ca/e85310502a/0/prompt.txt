check the logs at pi@mic for the doorbell-detector.service — hourly false-positive watch after the NCC scoring fix (commit 8006be1, threshold 0.4). Look at the last hour of journalctl for "Doorbell detected", buffer overflows, errors, and service restarts, and report findings briefly. This loop ends at 2026-07-04 20:00 local time: if the current time is at or after that, use CronList + CronDelete to delete this hourly job, give a final summary of the whole watch period, and stop instead of scheduling more checks.